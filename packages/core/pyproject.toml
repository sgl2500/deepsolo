[project]
name = "deepsolo-core"
version = "0.1.0"
description = "DeepSolo strategy warehouse and memory system"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "websockets>=12.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
